/**
 * Interface for a profile image.
 */
export interface IProfileImage {
    id: number; // Unique identifier for the profile image.
    name: string; // Display name of the profile image.
    src: string; // Source URL for the profile image.
}